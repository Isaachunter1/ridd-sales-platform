# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Secrets — never commit
.env
.env.*
!.env.example
*.secret
*secret*.json

# Build/runtime artifacts
node_modules/
dist/
.netlify/

# Local Claude tooling (launch.json etc.) — not part of the deploy
.claude/

# Customer account-level drill-down data — LOCAL ONLY, never deploy (PII)
accounts.json
marketing-demo.html

# Training assets are hosted in Supabase Storage (bucket: training), not git.
# Kept locally for convenient upload, but never committed (keeps the repo lean).
training/
BACKEND_with_warehouse_status.xlsx

# Deploy-time build artifact (tools/build.js regenerates per deploy)
app-*.immutable.js
